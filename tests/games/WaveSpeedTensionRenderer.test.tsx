import WaveSpeedTensionRenderer from '../../components/WaveSpeedTensionRenderer';
import { createGameTestSuite } from '../utils/game-test-factory';

createGameTestSuite('WaveSpeedTensionRenderer', WaveSpeedTensionRenderer, { tier: 'all' });
