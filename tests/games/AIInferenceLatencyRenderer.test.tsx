import AIInferenceLatencyRenderer from '../../components/AIInferenceLatencyRenderer';
import { createGameTestSuite } from '../utils/game-test-factory';

createGameTestSuite('AIInferenceLatencyRenderer', AIInferenceLatencyRenderer, { tier: 'all' });
