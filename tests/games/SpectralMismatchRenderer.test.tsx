import SpectralMismatchRenderer from '../../components/SpectralMismatchRenderer';
import { createGameTestSuite } from '../utils/game-test-factory';

createGameTestSuite('SpectralMismatchRenderer', SpectralMismatchRenderer, { tier: 'all' });
