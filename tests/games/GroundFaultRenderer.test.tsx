import GroundFaultRenderer from '../../components/GroundFaultRenderer';
import { createGameTestSuite } from '../utils/game-test-factory';

createGameTestSuite('GroundFaultRenderer', GroundFaultRenderer, { tier: 'all' });
