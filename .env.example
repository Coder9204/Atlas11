# ===========================================
# ENVIRONMENT VARIABLES FOR PROJECT ATLAS
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ===========================================

# Gemini API Key (for local development only)
# In production, use Firebase Functions to proxy API calls
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Security Note:
# - VITE_ prefixed variables are exposed to the browser (use only for Firebase client config)
# - Sensitive API keys (like GEMINI_API_KEY) should be stored in Firebase Functions environment
# - Use `firebase functions:config:set gemini.key="your_key"` for production
