PHASE 13 OF 20: CORE BUILD - FRAMEWORK & FOUNDATION
====================================================
Time Investment: Focused | Output: Working Foundation

Build the technical foundation before adding full content.

FRAMEWORK REQUIREMENTS:
----------------------
1. Screen/state management system
2. Navigation between screens
3. Progress tracking
4. User input handling
5. Feedback/response system
6. Animation system
7. Data/state persistence within session
8. Responsive layout (max 500px height)

BUILD ORDER:
-----------

1. NAVIGATION SHELL
   - Screen container
   - Progress indicator (dots showing current position)
   - Forward/back navigation
   - Screen transition animations

2. STATE MANAGEMENT
   - currentScreen tracking
   - User progress data (which concepts completed)
   - Variable values (sliders, choices)
   - Prediction history (what user predicted vs actual)

3. INPUT SYSTEM
   - Button click handlers
   - Slider change handlers
   - Drag handlers (if needed)
   - Touch/click detection

4. FEEDBACK SYSTEM
   - Visual response triggers
   - Text display components
   - Animation triggers
   - Success/error indicators

5. LAYOUT SYSTEM
   - Screen anatomy (header 50px / center 400px / footer 50px)
   - Responsive constraints
   - Spacing application
   - Typography application

IMPLEMENTATION:
--------------
Add to the renderer:

// State for screen navigation
const [currentScreen, setCurrentScreen] = useState(0);
const [userProgress, setUserProgress] = useState({});
const [predictions, setPredictions] = useState({});

// Navigation functions
const nextScreen = () => setCurrentScreen(prev => prev + 1);
const prevScreen = () => setCurrentScreen(prev => Math.max(0, prev - 1));

// Screen container with transitions
// Progress indicator
// CTA button component

FOUNDATION VERIFICATION:
-----------------------
Before moving on, verify:
- Can navigate forward through screens
- Can navigate backward
- Progress indicator updates
- State persists through navigation
- No console errors

OUTPUT:
-------
Update comment block:

/*
 * ENHANCEMENT PLAN - [GAME NAME]
 * Phase 13 Complete: Core Framework
 * - Navigation: implemented (currentScreen state)
 * - Progress: implemented (progress indicator)
 * - State: implemented (userProgress, predictions)
 * - Layout: 50/400/50 anatomy
 *
 * READY FOR PHASE 14
 */

Run: bash scripts/run_checks.sh
